# Static site (hand-written HTML/CSS in ./site). No build step.
# The previous Hugo/Wowchemy config is preserved in the repo but no longer built.
[build]
  publish = "site"
  command = "echo 'Static site — nothing to build'"
